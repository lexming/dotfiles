# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

# Default editor
export VISUAL="nvim"
export EDITOR="nvim"

# Shell history
export HISTSIZE=8000

